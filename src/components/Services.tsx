import { BookOpenCheck, Landmark, Cpu, FileText } from 'lucide-react';
import { useLanguage } from '../i18n/LanguageContext';
import { publicContent } from '../data/publicContent';
const icons=[BookOpenCheck,Landmark,Cpu,FileText];
export default function Services(){const {lang}=useLanguage();const c=publicContent[lang];return <section id="services" className="py-24 bg-corp-900"><div className="max-w-6xl mx-auto px-5 sm:px-8"><p className="text-primary-400 text-sm font-bold tracking-widest uppercase">Capabilities</p><h2 className="mt-4 text-4xl sm:text-5xl font-display font-bold text-white">{c.servicesTitle}</h2><p className="mt-5 text-gray-400 max-w-3xl leading-7">{c.servicesLead}</p><div className="mt-12 grid md:grid-cols-2 gap-5">{c.services.map((s,i)=>{const Icon=icons[i];return <article key={s.title} className="group p-7 rounded-2xl border border-white/10 bg-white/[.025] hover:border-primary-500/40 transition-colors"><Icon className="w-7 h-7 text-primary-400"/><h3 className="mt-7 text-xl font-bold text-white">{s.title}</h3><p className="mt-3 text-gray-400 leading-7">{s.body}</p></article>})}</div></div></section>}
