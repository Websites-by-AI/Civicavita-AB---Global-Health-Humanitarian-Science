# Local development only. Do not commit .dev.vars or .env files with real values.
ADMIN_EMAIL=admin@your-domain.example
ADMIN_PASSWORD=use-a-long-unique-password
JWT_SECRET=use-a-random-32-character-or-longer-secret
APP_ENV=development
OPENROUTER_API_KEY=
GEMINI_API_KEY=
